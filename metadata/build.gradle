apply plugin: 'kotlin'

dependencies {
    api Deps.kotlin.stdlib

    testImplementation Deps.kotlinx.coroutines.core
}

publishing {
    publications.all {
        pom {
            name = 'Coana MetaData'
            description = 'Kotlin Coroutine MetaData elements'
        }
    }
}
